# 배포 가이드라인

> **이 보일러플레이트는 배포 방식을 강제하지 않습니다.** 인프라는 프로젝트마다 다르기 때문에
> `deploy.sh` 같은 스크립트를 미리 넣어두지 않았습니다. 대신 **"이렇게 하면 좋다"는 모양**과
> 그렇게 하는 이유를 적어둡니다. 프로젝트 사정에 맞게 고쳐 쓰세요.
>
> 마이그레이션 쪽만은 실제로 들어 있습니다(`pnpm migrate:*`). 그것 없이는 배포 환경에
> 스키마를 올릴 방법 자체가 없기 때문입니다.

---

## 한 사이클

```
로컬                              서버 (ssh 접속 후)
─────                             ──────────────────
코드 변경                          git pull            어느 커밋을 올릴지는 사람이 정한다
migration:generate                 migrate:show:dev    무엇이 적용될지 먼저 본다
migration:lint      ← 작성 가드     migrate:run:dev     환경명을 입력하고 적용
커밋 · 푸시                        (빌드 · 재기동)      프로젝트가 정하는 부분
```

**순서는 마이그레이션이 먼저, 배포가 나중입니다.** 새 마이그레이션 파일이 서버에 있어야 하니
`git pull` 이 그 앞에 옵니다.

순서를 어겨도 조용히 지나가지 않습니다 — 미적용 마이그레이션이 있으면 **앱이 부팅을 거부**합니다
(`src/global/helpers/pending-migrations.helper.ts`). 마이그레이션을 잊은 배포는 그 자리에서
실패합니다.

---

## 왜 마이그레이션을 배포에 넣지 않나

배포 스크립트가 마이그레이션을 자동으로 돌리면 **스키마 변경이 아무도 안 본 채로 나갑니다.**

코드는 잘못 나가도 되돌리면 그만이지만, 스키마는 그렇지 않습니다. 컬럼을 지웠다가 되돌리면
컬럼은 돌아와도 **그 안에 있던 값은 돌아오지 않습니다.** 파티션이나 컬럼 rename 이 섞이면
더 그렇습니다.

그래서 `migrate:run:dev` 는 일부러 느립니다.

1. 적용 예정 목록을 먼저 보여주고
2. `y` 한 글자가 아니라 **환경명을 그대로 타이핑**하게 하고 (prod 를 dev 로 착각한 채 엔터를
   치는 것을 막습니다)
3. 적용한 뒤 결과를 다시 보여줍니다

`revert` 는 한 단계 더 요구합니다 — `revert-prod` 를 통째로 입력해야 합니다.

---

## 왜 서버에 들어가서 돌리나

로컬에서 배포 환경 DB 에 직접 붙지 않습니다. **서버에 ssh 로 들어가 거기서 돌립니다.**

- DB 가 사설 서브넷에 있어도 **서버는 이미 그 안에** 있습니다. 터널이 필요 없습니다.
- 실제 DB 자격증명이 개발자 노트북에 남지 않습니다. `envs/.env.dev` 는 서버에만 둡니다.
- 터널 방식의 고약한 실패 모드가 사라집니다 — 터널을 안 띄운 채 돌리면 에러도 없이 몇 분
  멈춰 있다가 타임아웃 납니다. 처음 접하는 사람에게는 원인을 알 방법이 없습니다.

**터널 스크립트는 넣지 않았습니다.** bastion 인지 VPN 인지 IP 허용목록인지가 프로젝트마다
달라서 보일러가 정할 수 없습니다. 로컬에서 원격 DB 를 봐야 하는 경우(앱 서버가 죽었을 때,
GUI 로 데이터를 볼 때)는 비상 수단으로 따로 마련하세요.

---

## 서버에 필요한 것

```
node          .mise.toml 이나 .nvmrc 로 버전을 고정하기를 권합니다.
              ★ 비대화형 셸에서는 버전 매니저가 안 잡히는 경우가 많습니다.
                ssh 로 직접 칠 때는 되는데 CI 나 cron 에서는 안 되는 함정입니다.
저장소 클론    환경마다 따로 두기를 권합니다. 한 디렉터리를 여러 환경이 쓰면
              dev 배포가 운영이 볼 빌드 결과를 덮습니다.
envs/.env.<env>   cp envs/.env.dev.example envs/.env.dev 후 값을 채웁니다.
프로세스 관리자   pm2 · systemd 등. 보일러는 정하지 않습니다.
```

---

## 환경이 어긋나는 것 막기

`scripts/_db-env.sh` 가 붙기 전에 **접속 대상을 크게 찍습니다.**

```
════════════════════════════════════════════════════════════
 환경 : dev
 대상 : db.internal:5432/myapp_dev
════════════════════════════════════════════════════════════
```

여기에 더해, `envs/.env.<env>` 에 `EXPECTED_DB_NAME` 을 적어두면 `DB_NAME` 과 대조하고
다르면 멈춥니다. **prod 설정을 `.env.dev` 에 잘못 복사한 경우**를 잡습니다 — 인자는 dev 인데
실제로는 운영 DB 에 마이그레이션이 나가는 상황입니다.

환경마다 DB 가 완전히 분리돼 있다면 비워두어도 됩니다. 비우면 검사하지 않고 대상만
보여줍니다.

> 한 DB 인스턴스를 여러 환경이 이름으로만 나눠 쓰고 계정까지 같다면, **이름 하나가 유일한
> 경계입니다.** 그때는 반드시 채우세요.

---

## 배포 스크립트를 만든다면

보일러에는 없지만, 만든다면 이런 것들을 확인하게 하기를 권합니다.

```
작업 트리가 깨끗한가        서버에서 파일을 고치지 않는다는 규칙의 집행
브랜치가 환경과 맞는가       develop 에 선 채로 prod 배포를 막는다
node 버전이 맞는가          같은 서버에 다른 프로젝트가 있으면 조용히 갈린다
빌드 결과가 실제로 있는가    없는 채로 넘기면 "script not found" 만 나온다
기동 로그에 환경·DB 가 맞나  뜬 뒤 눈으로 한 번 더 확인
```

**자동 배포(CI)를 붙일 때도 마이그레이션은 사람이 돌립니다.** 부팅 가드가 순서를 강제하므로,
마이그레이션을 잊은 자동 배포는 조용히 성공하지 않고 그 자리에서 실패합니다. CI 쪽에
마이그레이션 단계를 따로 만들 필요가 없습니다.

---

## 되돌릴 때

| | 비용 | |
|---|---|---|
| 앱만 되돌리기 | 싸다 | 이전 빌드로 재기동. 대부분 여기서 끝납니다 |
| 스키마 되돌리기 | 비싸다 | `migrate:revert:<env>` — 마지막 수단입니다 |

**둘을 같은 무게로 두지 마세요.** 사고 대부분은 앱만 되돌리면 해결됩니다. 스키마 revert 는
데이터가 함께 사라질 수 있습니다.

---

## 처음 배포하기 전 체크리스트

- [ ] `envs/.env.<env>` 를 서버에 두고 값을 채웠다
- [ ] `EXPECTED_DB_NAME` 을 채웠다 (또는 환경마다 DB 가 분리돼 있어 비워도 된다고 판단했다)
- [ ] `pnpm migrate:show:<env>` 로 **읽기만** 해보고 접속 대상이 맞는지 눈으로 봤다
- [ ] 로컬에서 `pnpm migration:lint` 를 돌려 파괴적 구문이 없는지 확인했다
- [ ] 되돌리는 방법을 알고 있다
